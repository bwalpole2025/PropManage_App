/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // Server actions are enabled by default in Next 15; keep config minimal.
  },
};

export default nextConfig;
